<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit message</title>
  </head>
  <body>
    <h3>Edit your chat</h3>
    <p>Id: <%=chat._id%></p>
    <p>This chat was sent from <b><%=chat.from%></b></p>
    <p>Received by <b><%=chat.to%></b></p>
    <form action="/chats/<%= chat._id%>?_method=PUT" method="POST">
      <textarea name="msg" id="" rows="7" cols="20"><%=chat.msg%></textarea>
      <br />
      <button>Edit</button>
    </form>
  </body>
</html>
